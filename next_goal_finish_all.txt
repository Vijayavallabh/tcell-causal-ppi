*** [SUPERSEDED 2026-07-21] DO NOT WORK THIS FILE. ***
*** Superseded by ./next_goal_after_gate_collapse.txt, which is authoritative. ***
*** This spec's Phases 4-6 assume the screening campaign produced valid graph-vs-no-graph comparisons. ***
*** It did not: StageALoss._graph annihilated the edge gates inside epoch 0, so feat-011 and feat-012  ***
*** are BLOCKED rather than unfinished, and cannot be closed by working the phases below.             ***
*** (The same notice appears at line ~174; it was too far down to be seen and is repeated here.)       ***

Finish every remaining feature in the EG-IPG project: close feat-006, feat-005, feat-008, feat-011, feat-012, and feat-013 as far as governance allows, in dependency order, without ever overstating a result. Repo /mnt/md0/IITM/BackUp/Home/vijayavallabh/tcell-causal-ppi, Python 3.12 + uv, PYTHONPATH=src. ./init.sh must stay green (currently 314 tests) at every phase boundary.

============================================================
WHERE THE PROJECT ACTUALLY STANDS (do not re-derive; verify if you doubt it)
============================================================
16 features: 10 done, 6 in-progress (feat-005, feat-006, feat-008, feat-011, feat-012, feat-013).
Recent commits: f1a00dd (5-seed campaign) -> e542d8c (review fixes + corrections) -> ae183ef (harness
lessons) -> 8a791c9 (docs propagation).

THE SCIENCE, CORRECTED. The 5-seed robustness campaign settled the graph question. Paired t on per-seed
Δsystema, n=5, one frozen fold (blocked_target_ood, 21,262/4,400), with family-wise correction over four
simultaneous contrasts (survives_family_wise requires BOTH Bonferroni and Holm):
  H2a  typed_static - expression_only    -0.0131  CI[-0.0190,-0.0072]  p=0.0036  bonf 0.0142  SURVIVES
  H2b  condition_gated - typed_static    +0.0112  CI[+0.0046,+0.0177]  p=0.0092  bonf 0.0369  SURVIVES
  promotion untyped_gnn - expression_only +0.0045 CI[+0.0011,+0.0079]  p=0.0208  bonf 0.0832  FAILS
  H1 vs no-graph  condition_gated - expression_only  -0.0019 CI[-0.0042,+0.0004] p=0.0847      FAILS
Per-config mean systema: untyped_gnn 0.0902 > expression_only 0.0857 > condition_gated 0.0838 >
typed_static 0.0726. AFTER MULTIPLICITY CONTROL NO GRAPH VARIANT RELIABLY BEATS NO-GRAPH. The typed graph
is reliably WORSE. The frozen H1 is at statistical PARITY with no-graph — it does NOT beat no-graph and
must NOT be described as below it. H2b only means gating repairs the damage typing did. Do not resurrect
the earlier, retracted phrasings ("H1 sits below no-graph", "the untyped GNN reliably beats no-graph") —
they were corrected in e542d8c and every doc now carries the corrected version.

H1's ONLY surviving positive claim is "beats the strongest eligible comparator": +0.0513 over the public
comparators (feat-010) and +0.0492 over the tabular bar (feat-006). The tabular margin is an UPPER BOUND —
the elastic-net bar did not converge (n_iter_max == max_iter, 6.4% non-zero coefficients).

============================================================
GLOBAL RULES — apply in EVERY phase
============================================================
- HONEST FRAME. This project's result is currently a NEGATIVE. Your job is to finish the work and report
  what is true, never to rescue the premise. A phase that produces another negative is a SUCCESSFUL phase.
  Do not tune to reach a positive. If a result weakens H1, say so plainly and prominently.
- A COMPARISON YOU DID NOT COMPUTE IS NOT A RESULT. If you state "A beats / trails B", a test of A-vs-B
  must exist in the code that produced the report. Never read a comparison off two marginal means or
  overlapping intervals. (This exact error was made and corrected here — see AGENTS.md.)
- MULTIPLICITY. When several contrasts/hypotheses are tested at once, report a family-wise corrected p and
  require it before calling anything "reliable". Record BOTH Bonferroni and Holm so the method cannot be
  chosen after seeing which one rescues a claim.
- A GUARD WHOSE INPUT IS A CONSTANT CAN ONLY CONFIRM. For every fence you write or rely on, ask: what input
  would make this FIRE? If none can exist, it is decoration. Absence of evidence must report None/unknown,
  never a pass. A degenerate case (zero variance, empty set, missing file) is UNDECIDABLE, not significant.
- GUARDS HONORED END TO END, INCLUDING THE EXIT CODE. Trace every guarded value to output, JSON, downstream
  gate AND the process exit status. A run that prints "NOT comparable" must not exit 0.
- A BAR MUST BE FIT WELL ENOUGH TO BOUND. For any baseline/comparator that a result must CLEAR, record its
  convergence/sparsity evidence in the ARTIFACT (not stdout). An under-fit bar INFLATES the very margin it
  exists to bound; "more regularisation only weakens it" is the safe direction for a competitor and the
  WRONG one for a floor.
- PRESENCE IS NOT FRESHNESS; PROVENANCE IS NOT COMPARABILITY. Gate on run-id/mtime/fold-size recorded IN
  the artifact, never on a file merely existing, and never difference two numbers from different folds.
- TESTS: any test pinning a correctness claim must be watched FAILING first; for correctness-critical code
  CONSTRUCT an input that defeats it (AGENTS.md adversarial-input gate). Mutation testing is necessary, not
  sufficient.
- COMMITS: commit ONLY when I ask. One commit per phase, carrying the DoD triad (feature_list.json +
  progress.md + session-handoff.md) together with the code. feature_list.json evidence is APPEND-ONLY: never
  rewrite, append a dated block; a programmatic edit must round-trip byte-identically first
  (json.dumps(d, indent=2) + "\n", ensure_ascii default True) and be diff-checked (~1 line per feature).
  No attribution trailers. Branch main.
- HARD SAFETY: the sealed challenge split (5,608 rows) is SEQUESTERED and UNOPENED — do NOT run
  evaluation/sealed_eval.py, ever. NEVER run run_module0.py (DESTRUCTIVE, hook-blocked). The box is SHARED:
  nvidia-smi before claiming any GPU, and report contention honestly (gpu_hours is a contended wall-time
  proxy, not clean compute).
- BEFORE ANY LONG GPU RUN (> ~1 h) or ANY NEW DEPENDENCY: stop and show me the plan — what runs, on which
  GPUs, the GPU-hour estimate with its basis, the early-stop rule, and the statistic you will report. For a
  dependency, state the package, its size, and what it pulls (weights? network?). Let me approve first.

============================================================
PHASE 1 — feat-006 Simple Baselines: finish the tabular family and TRY TO BREAK H1
============================================================
Remaining per its own evidence: CatBoost/gradient-boosting, TabPFN/TabICL, and a CONVERGED elastic-net.
(1) Make the elastic-net bar converge (max_iter/tol, or scale the target) and re-report the margin — it can
only shrink. (2) Add a CatBoost/gradient-boosting baseline. (3) Assess TabPFN/TabICL feasibility HONESTLY
against this fold (21,262 train x 1412 features -> 128 regression outputs) BEFORE installing: state its real
sample/feature/output limits; if it needs PCA, subsampling or a per-output loop, say what that costs and
what the comparator then MEANS. (4) Score every bar on the SAME frozen fold via the SAME
response_metric_suite and re-rank vs the frozen H1 on systema.
Frame: the job is to BREAK H1's comparator clause, not defend it. The report calls this a near-null-signal
regime where strong TABULAR models are the real threat — if one closes or reverses the margin, that IS the
result. These are feat-006 BASELINES: they consume NO feat-010 comparator-family cap, are not registered,
and keep kind="baseline". Leakage fence: fit on TRAIN responses only; val features are frozen static node
features; train_mean from train. Report the 385 train / 91 val off-graph rows carrying an all-zero feature
vector; changing that handling changes every bar's score.
DONE WHEN: every bar's convergence/sparsity evidence is persisted in the artifact; the margin is restated
with an explicit statement of whether it SHRANK; feat-006 flips to done or its remaining gap is named.

============================================================
PHASE 2 — feat-005 Latent Program Extraction: the basis study
============================================================
Remaining per its evidence: "the method x K comparison metrics + shallow-VAE basis still remain." The
extraction machinery is DONE and the sparse_pca production loadings are FROZEN — only the STUDY remains.
Run the 4-method x 4-K (64/128/256/512) comparison on reconstruction / sparsity / stability, plus a
shallow-VAE basis. CRITICAL: the production basis is frozen and downstream results depend on it — this is a
STUDY that characterises the choice, NOT a licence to swap the basis. If a different method/K looks better,
report it as a finding and STOP; changing the frozen basis would invalidate every result above and is my
call, not yours. Stability needs a defined resampling protocol — state it before running.
DONE WHEN: the comparison table exists with a named stability metric, the shallow-VAE basis is built and
scored, the frozen production basis is unchanged, and feat-005 flips to done.

============================================================
PHASE 3 — feat-008 EG-IPG Model: the Stage-B fit loops (THE CRITICAL PATH)
============================================================
Remaining per its evidence: "the Stage-B calibration + rationale FIT loops (both loss modules exist, no fit
loop), and the near-null-signal freeze gate." (Its older line about feat-007 being not-started is STALE and
already marked superseded — feat-007 is done.)
Build: (a) the Stage-B calibration fit loop, (b) the rationale-head fit loop, (c) the near-null-signal
freeze gate. This is the largest remaining build and it is what BLOCKS feat-012 — audit_rationale needs the
frozen H1 model PLUS a fitted rationale head.
Frame: in a near-null-signal regime a calibration/rationale head can look good by fitting noise. Whatever
you fit, evaluate it against a matched-random control and report the control, not just the headline.
Gate: state the training plan (data, epochs, GPU-hours, early-stop, what "converged" means) before the run.
DONE WHEN: both fit loops train and are tested red-first, the freeze gate exists, ./init.sh green, and
feat-008 flips to done.

============================================================
PHASE 4 — feat-011: resolve the "full EG-IPG" ambiguity, then close
============================================================
Screening + promotion + the 5-seed robustness campaign are DONE. But feat-011's description says screen
"expression-only, typed static graph, condition-gated typed graph, AND full EG-IPG" — four members — while
the campaign screened three plus the untyped-GNN diagnostic. ASK ME which is intended; do NOT guess:
  - if "full EG-IPG" == condition_gated (Stage-A), feat-011 is a status flip to done;
  - if it means Stage-A + Stage-B (i.e. it needs Phase 3 first), then a re-screen of that member on the
    SAME frozen fold is required, paired across seeds, with the same multiplicity discipline.
This ambiguity is exactly the kind of thing that was guessed wrong here before. Bring me the question.

============================================================
PHASE 5 — feat-012 Predictive-Rationale Audit (hard-blocked by Phase 3)
============================================================
"The campaign itself remains UNRUN." The machinery (FaithfulnessTester, MatchedRandomSampler,
audit_rationale) exists and has been validated at real scale on an UNTRAINED model — that was a machinery
check, NOT a result, and the report says so. Once Phase 3 lands, run the REAL campaign on the frozen H1 plus
the fitted rationale head: necessity / sufficiency / minimality / stability against matched-random and
structural-OOD controls.
Frame: the frozen H1 is at PARITY with a no-graph model. A rationale audit of a model with no demonstrated
graph advantage can still be reported, but it cannot be presented as evidence FOR the graph — say what it
does and does not license. If a graph-mutating ablation control is used, remember a write through
tensor.data bypasses cache invalidation: call invalidate_graph_caches(graph).
DONE WHEN: the real campaign has run, controls are reported alongside every headline number, and feat-012
flips to done.

============================================================
PHASE 6 — feat-013 Reproducibility: everything EXCEPT the sealed step
============================================================
"The campaign itself remains UNRUN." Build the manifest from a REAL run (independently-frozen expected
hashes where they exist; self-derived entries must be LABELLED as not a reproduction test) and author the 11
fallacy probes from real diagnostics.
BOUNDARY — READ THIS TWICE: feat-013 CANNOT reach done through you. Its terminal state needs a sealed
confirmatory decision, and opening the sealed challenge split is TEST-STEWARD-ONLY and is never done here.
verify_reproducibility returning CANNOT_VERIFY is the CORRECT answer, not a failure to fix. Take feat-013 to
"everything except the sealed step", record precisely what remains and who must do it, and leave it
in-progress. The same boundary is why H1's confirmatory verdict is not yours to produce: everything above is
DEVELOPMENT-fold evidence.
DONE WHEN: the manifest + probes exist, the honest CANNOT_VERIFY verdict is recorded with its reason, and
the handoff names the steward-only remainder.

============================================================
ORDERING, AND WHAT TO DO WHEN
============================================================
Phase 1 and Phase 2 are independent — either order, both cheap. Phase 3 is the critical path. Phase 5 is
hard-blocked by Phase 3. Phase 4 must be ASKED before it is planned. Phase 6 is independent and cheap.
Suggested: 1 -> 2 -> 3 -> (4 question) -> 5 -> 6.
Work ONE phase at a time. At each phase boundary: ./init.sh green, then show me the result and STOP for the
commit. Do not start the next phase's long run without the gate above.

VERIFY (every phase): ./init.sh green; promoted.json UNCHANGED unless I explicitly say otherwise; the sealed
split untouched; DoD triad updated; evidence appended (never rewritten) with a diff-checked feature_list;
and every claim in the write-up traceable to a computed number, with multiplicity controlled and any
under-fit bar or non-comparable fold flagged LOUDLY.

[SUPERSEDED 2026-07-21] This spec assumes the screening campaign produced valid graph-vs-no-graph
comparisons. It did not — the edge gates were driven to ~1.3e-07 inside epoch 0 by an unnormalised
L_graph. Phases 4-6 below are void. Work ./next_goal_after_gate_collapse.txt instead.
