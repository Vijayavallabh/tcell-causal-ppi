=======================================================================
AUTONOMOUS GOAL SPEC — the A- and B-series are closed; this is what their answers opened.
Rewritten 2026-08-19. D-series appended AND CLOSED 2026-08-21, with C1a.
Branch icbinb-multidataset-2026-08-03. 4x A100 80GB, SHARED.
ICBINB-BIO deadline 29 Aug 2026 AoE. RE-VERIFIED 2026-08-21: unchanged and STILL marked tentative.

*** RAIL 4 FIRED 2026-08-21. READ RESULTS_SUMMARY.md's TOP BLOCK BEFORE TOUCHING THE PAPER. ***
D4's audit found Amendment 3.1 had promised a pooled estimate over the K=128 subset alongside the
all-datasets one, and that it was never computed while the paper claimed it WAS reported. Running it
is free. The two disagree: all eight give the untyped margin +0.0091 [-0.0024,+0.0207] p=0.12; the
five K=128 datasets give +0.0141 [+0.0041,+0.0241] p=0.0056, all five positive, clearing Bonferroni
AND Holm at m=4. The graph HELPS in a pre-registered analysis nobody had run. Snapshotted to
data/results/replication/pooled_k128_subset.json, flagged, and the null was NOT rewritten around it.
It does NOT touch the headline null (h1 is unchanged at k=1, +0.0033, p=0.90). It DOES undercut the
body's "sign we cannot predict" sentence: the only negative anywhere is Norman, and Norman ran at
K=16. Whether the body argument changes is a HUMAN call, not an agent's.
=======================================================================

This file is your standing authority. Work for hours or days unattended. Never ask, wait, or present
options: at each fork take the best choice on your own judgement and LOG IT WITH THE REASON. On
failure, skip and move on. Snapshot every result to disk.

The paper is FINISHED and submission-ready at 8pp with every A-item integrated. Nothing below is
required for it to be submitted. Everything below answers a question the A-series raised, and the first
item is the one the paper's own argument now points at.

=======================================================================
RAILS — never cross
=======================================================================
1 NEVER run evaluation/sealed_eval.py; NEVER open the sealed challenge split.
2 READ-ONLY: data/results/screening/, data/results/screening_lambda0/, data/splits/, promoted.json,
  paper/figures/eg_ipg_architecture.pdf, and every landed results root. Every run gets a FRESH root,
  sha256-manifested before and after where it seeds from a frozen one.
3 Paper edits additive and reversible, marked `% AUTO <date>`. Body must stay EXACTLY 8pp with
  0 errors / 0 overfull / 0 undefined. Overflow is fixed by MOVING to an appendix, never by cutting.
  ./check_paper.sh checks all four gates in one command; the body gate is "References open page 9".
4 CONTRADICTION STOP: if the graph HELPS and survives Bonferroni AND Holm anywhere, do NOT rewrite the
  null around it. Snapshot, flag atop RESULTS_SUMMARY.md, continue. This has fired four times and each
  one is in the paper.
5 Headline a result only at n>=4 seeds for BOTH arms of the contrast. Fewer is preliminary and must be
  labelled so, with its n.
6 Branch only: no commit to main, no PR. Explicit paths only, never `git add .` or `-a` (shared
  checkout). Commit message BEFORE the `--`.
7 docs/replication-prereg.md is FROZEN. Amend only by dated append, and only BEFORE running the thing
  it governs. A factual record of what was run goes in RESULTS_SUMMARY.md, not the pre-registration.
  Amendments 4, 4a, 4b, 5 and 6 each went in before the run they govern. Keep it that way.

=======================================================================
WHERE THE A-SERIES LEFT THE ARGUMENT — read this before choosing work
=======================================================================
On the frozen fold at n=5, ordered by systema:

    untyped_gnn      0.0904     plain topology, no typing, no gating   <- the best graph arm
    expression_only  0.0857     no graph
    condition_gated  0.0838
    typed_permuted   0.0791     per-relation weights, RANDOM labels
    typed_shared     0.0730     one message module for all relations
    typed_static     0.0726     the true typing

A1 asked which part of the typing costs the 0.0176 between untyped and typed, and eliminated both
candidates. Cutting the message parameters fourfold (34% of the model) moves it +0.0004. Randomising
every relation label, edge counts preserved exactly, moves it +0.0065 and does not clear correction.
So it is NOT the parameter count and NOT the annotation's content.

A2(a) showed the instrument is fine: an injected graph signal is recovered at 0.02 response SDs and a
scrambled-neighbourhood control at 0.40 is not recovered at all.

A4 removed the reason we had stopped looking. The 14-cell architecture search that "refuted"
per-relation normalisation spans 0.0089 across ALL its cells while the gap under study is 0.0176; it
was not resolving these arms, so nothing was refuted inside it.

WHAT IS LEFT is the message FORM, and it is a short list: degree normalisation, signed messages, the
per-edge feature term, complex-membership nodes, and the residual FFN. That is B1, and it is the only
item here that can change what the paper concludes.

=======================================================================
THE AUTONOMOUS WORK, in priority order
=======================================================================

--- B1. WHICH COMPONENT of the message form does the damage?  HIGHEST VALUE -------------------
Walk the bridge from typed_static to untyped_gnn one component at a time, each arm removing exactly
one thing, paired per seed against the LANDED typed_static lanes on the frozen fold. n=4 minimum.

  B1a  DEGREE NORMALISATION.  *** CLOSED 2026-08-20 17:50. IT IS THE ROUTE. ***
       Arm typed_gcnnorm wired (screening.py + run_screening.py _DIAGNOSTIC), pre-registered as
       Amendment 7, root data/results/screening_b1, seeds 0-4, cards 0/1/3 (CUDA index; card 2 was at
       22 GiB and was skipped). Watch: data/logs/b1_gcnnorm.nohup.log and data/logs/b1/*.log.
       run_b1_finalise.sh is waiting on the lanes and will merge the registry and run b1_report itself.
       COST WAS UNDER-ESTIMATED at ~32 GPU-h: 5 lanes x 20 epochs at the 38-90 min/epoch this box
       actually delivers is 60-150 GPU-h, and with 3 cards that is two waves. Re-measure from the first
       epoch line rather than trusting either figure.
       RESULT: D3 = +0.0139 [+0.0029,+0.0248], 5/5 seeds, 79% of the +0.0176 gap. The deficit is the
       UNNORMALISED SUM, not the typing. But gcnnorm vs no-graph is +0.0008 (p=0.71): repairing the
       encoder reveals NOTHING underneath, so the null stands and rail 4 does NOT fire.
       CAVEAT, do not lose it: m=1, so both corrections are the IDENTITY. Raw p=0.0245 clears at m=2,
       FAILS at m=3 and m=4. The robust parts are 5/5 seeds and the 79% share, not the p.
  B1b-d DECIDED 2026-08-20: NOT RUN, on power. The residual they would partition is
       untyped_gnn - typed_gcnnorm = +0.0037, against an MDE of 0.0096 at n=5 -- 2.6x the whole
       residual before each arm's share of it. 21 seeds to detect the whole residual, 77 for half.
       Three arms at n=5 would be underpowered by construction: the A4 mistake. Stopping also preserves
       D3's p-value and that is recorded rather than hidden. To overturn this, run them and re-correct
       at m=4 per Amendment 7.3, where D3 does not clear.
       `StaticTypedGraphEncoder(graph, g2i, norm="gcn")` ALREADY WORKS — _RelMessage supports
       ("add","mean","gcn") and StaticTypedGraphEncoder defines no __init__, so it inherits the
       keyword. Zero new encoder code; the wiring is the five-line pattern typed_shared used
       (a name in screening.py, a factory entry, the _DIAGNOSTIC list in run_screening.py).
       WHY FIRST: it is the one lever A4 un-refuted, it is the difference the arch search was built to
       test and could not resolve, it is free to implement, and it is the only structural difference
       between these encoders that is a single keyword.
       It is also the mechanism already MEASURED to depress the mean gate tenfold (0.03 and 0.12
       against 0.73) in that search, so there is a prior reason to expect it matters.

  B1b  SIGNED MESSAGES.  ~32 GPU-h.  Replace tanh(W_sign h)*relu(W_mag h) with a plain linear message.
       Needs a small subclass of _RelMessage. This is the largest remaining structural difference from
       GCNConv after normalisation.

  B1c  THE PER-EDGE FEATURE TERM.  ~32 GPU-h.  Drop u_r(edge_attr) from the message.

  B1d  COMPLEX-MEMBERSHIP NODES.  ~32 GPU-h.  Run the typed encoder over protein nodes only.

DECIDE IN ADVANCE, and write it into the prereg as a dated append BEFORE running anything:
  - Primary per arm: (arm) - typed_static on systema, paired per seed, n>=4.
  - Family = the arms actually run in the campaign, m = that count. Bonferroni AND Holm, both required.
  - The route is the component whose removal recovers the largest share of the 0.0176 gap to
    untyped_gnn AND clears both corrections. If several clear, report all of them and claim none
    exclusively — the components are not independent and this design cannot separate interactions.
  - If NONE clears, that is a result: the deficit is distributed across the message form rather than
    localised in one component, and the paper says so instead of naming a culprit.
  - A component whose removal makes things WORSE is reported as such, not folded into "no effect".
STOP RULE so this does not become an unbounded search: run B1a first and read it before launching
B1b-d. If B1a alone recovers most of the gap, the remaining arms are confirmatory, not exploratory,
and should be run at that framing.
Do NOT re-run confidence pruning. A4 un-refuted normalisation specifically; pruning was refuted by the
same underpowered search but there is no positive reason to expect it, and it costs the same.

--- B2. WHERE in the gene ranking does the graph win and lose?  *** CLOSED 2026-08-19 *** --------
A3 found the untyped contrast is corrected-significant NEGATIVE on each perturbation's top-20 DE genes
and corrected-significant POSITIVE over all 10,282, crossing zero between the 250th and 500th. The
paper states the crossover; it does not say what happens on either side of it.
CLOSE WITH: the paired per-seed contrast computed within DECILES of |observed response|, on the stored
predictions under data/results/predictions/. Reuse rescore_external's alignment and the k-sweep
machinery. That turns "it flips" into "the graph helps on genes ranked X to Y and hurts on Z", which is
a statement about what a PPI prior is actually good for and the most portable thing in the paper.
CHEAP, needs no GPU, and it is the natural first item if the cards are contended.

--- B3. Does the wrong-prior damage SCALE?  *** ANSWERED WITHOUT RUNNING IT, 2026-08-19 *** ----
The scrambled control's increment over the zero point is -0.0050 ([-0.0083,-0.0016]): injecting
structure that follows the WRONG graph destroys the real benefit rather than merely failing to help.
That claim currently rests on ONE condition at ONE delta.
DO NOT RUN THE delta=0.10 RUNG. The power calculation was done first and it cannot answer the question.
Artifact data/results/a2_ladder/b3_power.json, module screening/b3_power.py, all numbers re-derived from
the landed ladder's own intervals rather than assumed:
  - proportional damage predicts -0.0012 at delta=0.10; constant damage predicts -0.0050.
  - MDE at n=4 is 0.0044 on the control's own seed spread and 0.0082 on the injected rungs' median,
    and 0.0080 once the enlarged ladder family is corrected for.
  - So a delta=0.10 rung cannot distinguish proportional damage from ZERO under any measured spread,
    and cannot clear correction even if the damage were CONSTANT. Detecting the proportional prediction
    needs 25 seeds on the control's spread, 81 on the rungs'.
An underpowered second rung would have produced a null that meant nothing - which is precisely the
mistake A4 caught the 14-cell architecture search making, and it would have cost ~22 GPU-h to repeat it.
WHAT WAS DONE INSTEAD, free: the paper's claim is now labelled with the magnitude it was measured at.
app:floor says the damage is measured at one injection size, that the scaling is left open, and what
settling it would cost. That is the honest version of the sentence B3 was going to check.

--- B4. Tighten the floor below 0.02?  CONSIDERED AND DEPRIORITISED ----------------------------
The floor is reported as "at or below 0.02" because 0.02 is the smallest rung we injected, so smaller
rungs look like the obvious next step. They are not, and the reason is in the artifact: the post-hoc
increment shows the injection's own contribution is not separable from the pre-existing +0.0048 benefit
below delta=0.40 at four seeds. A delta=0.005 rung would therefore mostly re-measure the benefit that
is already there. If this becomes worth doing, buy SEEDS at an existing rung rather than new smaller
rungs. Recorded so the next agent does not spend 40 GPU-hours rediscovering it.

--- B5. The abstract sentence that did not fit.  *** CLOSED 2026-08-19 *** ---------------------
The measured floor and its scrambled control are the answer to the standing objection to any null, and
they are the strongest thing the paper can put in an abstract. The sentence was drafted, did not fit,
and was reverted on 2026-08-19; it currently lives in Limitations and the checklist only.
CLOSE WITH: free roughly 250 characters of body by MOVING one more duplicated block to an appendix
(rail 3 permits moving, never cutting; the replication narrative has already gone this way and the
"Two narrower limits" paragraph is the next candidate), then add:
  "An injected graph signal is recovered at $0.02$ response SDs; the same signal over a scrambled
   graph is not, so the null is bounded by a measured instrument."
GATE: ./check_paper.sh must PASS, and abstract_plain.txt must be REGENERATED from main.tex afterwards
— it is the text SUBMISSION.md tells a human to paste into the portal, and it has silently gone a full
campaign out of date once already.

--- B6. Harness and docs consistency.  *** CLOSED 2026-08-19 *** -------------------------------
docs/reproduction.md predates the A-series and does not mention power_simulation, external_metrics,
rescore_external, arch_search_power, rationale_audit_bound, inject_signal, ladder_report, a1_report,
check_paper.sh, run_a1_mechanism.sh, run_a2_ladder.sh or run_ladder_finalise.sh. Add them with the
command that reproduces each artifact. Then run ./init.sh and fix whatever it reports.

--- C1b. The typed arm's OWN detection floor.  ~200 GPU-h.  THE ONE REAL EXPERIMENT LEFT -------
(C1a below is its pre-registration, which is free and must land first. C1b is the lanes.)
CREATED BY THE DEVIL'S ADVOCATE PASS, 2026-08-20. The paper's headline null is about the typed, GATED
arm. The measured floor is not: Amendment 6.5 registered the ladder on untyped_gnn against
expression_only, on cost (2.3-2.5 GPU-h per lane against 7.5-16) and on sensitivity, and closed with
"this is a bound on the pipeline's sensitivity, not on the typed encoder's specifically, and it will be
labelled that way". It is now labelled that way in the abstract, Limitations and app:floor, twice
saying the typed arm's own floor is UNMEASURED.

That is a real gap and it is measurable. The a-fortiori argument only runs one way: the untyped arm
detects at 0.02 response SDs, so weaker arms need AT LEAST that, but the typed arm's actual floor could
be much higher, and this paper's own evidence says it is the poorer detector (h2a -0.0120; B1a puts
most of that on the unnormalised sum). If the typed arm's floor turned out to be, say, 0.20, the
headline null would be a statement about an instrument that cannot see what it was pointed at.

CLOSE WITH: re-run the existing ladder with ARMS="expression_only condition_gated" over the six built
rungs under data/intermediate/inject, 4 seeds, through run_a2_ladder.sh. The rungs already exist, so
this is lanes only. 24 condition_gated lanes at 7.5-16 GPU-h is 180-380 GPU-h; three A100s are free.
PRE-REGISTER FIRST as an extension of Amendment 6, stating the primary (condition_gated minus
expression_only per rung), the family, and what each outcome means, BEFORE any lane starts.
WHAT IT BUYS: either the null is bounded by an instrument demonstrably able to see a signal of that
size, which is the strongest form of the paper's central claim; or it is not, which is a bigger finding
than anything else left on this list and one the paper would have to carry.

--- C1a. WRITE C1's PRE-REGISTRATION NOW.  *** CLOSED 2026-08-21. Amendment 9 has landed. *** ---
Split out 2026-08-21. Rail 7 forbids starting C1's lanes before the amendment lands, so the amendment
is the part that can be done unattended and the lanes are the part that spends 180-380 GPU-h. Writing
it costs nothing, commits nothing, and turns launching C1 into a one-command decision instead of a
design session. Pre-registering something that is never run is harmless; running it unregistered is
not recoverable.
CLOSE WITH: a dated append to docs/replication-prereg.md as Amendment 9, extending Amendment 6, fixing
BEFORE any lane: the primary (condition_gated minus expression_only per rung, on systema, paired per
seed, n=4), the family (the six injected conditions, m=6, Bonferroni AND Holm, both required), the
floor rule and the monotonicity red flag copied from 6.7 so the two ladders are read the same way, the
permuted control's veto, and what EACH outcome means, including the one that hurts.
STATE THE UNCOMFORTABLE OUTCOME IN ADVANCE, in the amendment, in writing: if the typed arm's floor
lands far above 0.02, the paper's headline null is a statement about an instrument that could not see
what it was pointed at, and the paper carries that. Committing to it before the numbers exist is the
only thing that makes the answer worth anything.

*** CLOSED 2026-08-21, commit 5f3d1e9. Amendment 9 is in docs/replication-prereg.md, dated append,
before any lane exists, so rail 7 is satisfied and C1b is unblocked. It fixes the primary, the family
at m=6 with both corrections, the floor rule and monotonicity red flag copied from 6.7, the permuted
control's absolute veto, the zero point excluded from the family per 6.4, and 9.8 states the
uncomfortable outcome in writing along with the reverse commitment.
IT ALSO CAUGHT A SETTING THAT WOULD HAVE VOIDED THE WHOLE CAMPAIGN. run_a2_ladder.sh does not set
LAMBDA_GRAPH, so it uses the config default 0.01. That is harmless for Amendment 6's arms, which have
no live gate. condition_gated is the ONLY arm whose gate is live, the gate collapses toward 1e-7 as
lambda rises, and lambda_graph=0 is what reproduces live gates at 0.76 -- which is why the paper's
headline family sits on the REPAIRED screening_lambda0 root. At the default, all 24 lanes would have
trained with suppressed gates, measured a floor for a different model than the null describes, and
tripped Amendment 3.4's own kill criterion (gate <= 1e-3 = UNDECIDABLE). Amendment 9.2 pins
lambda_graph=0 and makes gate health a reported quantity.
BOTH PREREQUISITES ARE DONE, commit 3b48889, and one of them was NOT what this file said:
  1 *** AN ENVIRONMENT VARIABLE DOES NOTHING. *** config.LAMBDA_GRAPH is a plain module constant, so
    `export LAMBDA_GRAPH=0` is silently ignored. The override is the CLI flag `--lambda-graph 0`, and
    run_a2_ladder.sh does not pass it. That is why C1b is run_c1_ladder.sh and not
    ARMS="..." ./run_a2_ladder.sh. run_screening's own --help states the consequence: at the default
    0.01 "the unnormalised per-edge sum is ~103x the response term and annihilates the gates inside
    epoch 0". Verified on the live command line after launch.
  2 ladder_report.py now takes --arm and --reference-root, with three tests each written to FAIL if
    the parameter is ignored. The DEFAULTS reproduce Amendment 6's landed floor.json exactly, checked
    rung by rung. A regression the existing tests caught, worth knowing: binding REFERENCE_ROOT as a
    default argument froze it at import and silently defeated their monkeypatch -- producing not an
    error but a plausible number against the real root. It resolves at call time now.

--- C2. Bookkeeping the paper work of 2026-08-20 -----------------------------------------------
Done in-session and NOT reflected here until now: the devil's advocate pass
(docs/devils-advocate-2026-08-20.md), the full section-by-section rewrite, the number-drift verifier
(paper/icbinb/verify_numbers.py plus numbers.snapshot.json, run --check after ANY paper edit), the
ICBINB content-spec compliance pass, and the section restructure to Problem / Proposed approach /
Observed outcome / Reason for failure.

=======================================================================
D-SERIES. SUBMISSION HARDENING. Zero GPU. All four are yours to finish unattended.
=======================================================================
Written 2026-08-21 after auditing what the 2026-08-20 rewrite left ungated. The paper is submission
ready and none of this changes a result. These are the failure modes still live on filing day, ordered
by how badly each one bites if it fires.

--- D1. GATE THE DOUBLE-BLIND COMPLIANCE.  *** CLOSED 2026-08-21, c4c8eb6 + 23330d6. *** --------
GATE6 in check_paper.sh runs paper/icbinb/check_anonymity.py over main.tex, main.bbl, the RENDERED
pdftotext, the PDF metadata, and (--repo) all 245 tracked files, because the venue blinds LINKED
material and 4open serves tracked content unchanged. Verified clean everywhere first, so it keeps a
clean thing clean. THE DENY-LIST IS DELIBERATELY NOT IN THE TRACKED SCRIPT -- writing the author's
name into check_paper.sh to search for it would have made that file the SEVENTH deanonymising string
-- so identity terms are derived at runtime from git config and the checkout path, and only
structural patterns (any email, any code-host URL, an ORCID, an absolute home path, a non-anonymous
\author, non-empty PDF /Author /Title) are written down. github.com REJECTED,
anonymous.4open.science ACCEPTED, commented in both files so nobody inverts it. --self-test catches 8
planted leaks; a leak planted in the real main.tex was caught end to end and reverted with sha256
confirmed identical. Missing pdftotext/pdfinfo is a REFUSAL, not a pass. PDF timestamps carry IST:
examined, left alone, reason recorded in SUBMISSION.md.
NOTHING in the harness checks anonymity. check_paper.sh gates the build, the page budget and abstract
drift, and not one of its checks looks for a NAME; verify_numbers.py checks numbers only. The repo
scrub is dated 2026-08-11 and the ENTIRE paper was rewritten section by section on 2026-08-20 with no
anonymity re-check afterwards.
STATE AS OF 2026-08-21, verified rather than assumed: main.tex is clean, `pdftotext main.pdf` is
clean, and pdfinfo reports an EMPTY /Author and /Title with Creator "LaTeX with hyperref". So this is
a gate to keep a clean thing clean, not a leak to plug. That is exactly when it is cheap to add.
CLOSE WITH: a new gate in check_paper.sh (its last shell variable is GATE5, so this one is GATE6),
failing the build on any hit across main.tex, main.bbl and `pdftotext main.pdf -`:
  - the strings the 2026-08-11 scrub removed (read the list out of SUBMISSION.md, do not re-invent
    it), the author name, the email, the institution, and any github.com/<user> URL;
  - a non-empty /Author or /Title from pdfinfo.
IT MUST NOT REJECT the anonymous.4open.science placeholder. That URL stays a placeholder until a human
creates the mirror (NOT AUTONOMOUS, step 1), so the gate ACCEPTS anonymous.4open.science and REJECTS
github.com. Write that distinction as a comment beside the deny-list or the next agent will "fix" the
gate in the wrong direction.
WHY FIRST: it is minutes of work, and it is the only failure on this entire file that CANNOT be
repaired after the deadline. Every other item here makes the paper worse. This one is a desk reject.
MINOR, decide it either way and record which: the PDF timestamps carry IST, a weak locality signal,
standard in every LaTeX submission, removable with \pdfinfoomitdate. Not a compliance failure. Say
what you decided rather than leaving it unexamined.

--- D2. EXTEND ARTIFACT AGREEMENT FROM 3 TABLES TO 11.  *** CLOSED 2026-08-21, 55d3325. *** ------
All eleven tables are now in a registry with per-table coverage (--coverage). Nine are fully
re-derived; tab:causes and tab:repl are PARTIAL and say so with the reason, as named no-ops. A
missing artifact now FAILS as SKIPPED instead of passing through `if path.exists():`. Wired as GATE7,
bare rather than --check, so it can only fail on a real disagreement.
IT FOUND A REAL ERROR: tab:floor2's Bonferroni p for the delta=0.40 rung read 0.023 where floor.json
gives 0.022487, which rounds DOWN. Every other rung already rounded correctly. Invisible to the
inventory by construction and missed by two rounds of hand-checking.
CORRECTION TO THIS FILE'S OWN MAPPING: D2 said tab:family was "the n=7 reference family". It is NOT.
It is n=5 on the REPAIRED screening_lambda0 root. That root and data/results/screening/ agree on
three arms of four and differ exactly where the gate repair moved things (cg 0.0848 vs 0.0838, h1
-0.0009 vs -0.0019), so checking against the wrong one reports a correct number as an error.
verify_numbers.py has two checks and they are not equally strong. The literal INVENTORY covers every
number in the body and catches one that changes or vanishes during an edit. The ARTIFACT AGREEMENT
check re-derives numbers from JSON and catches one that was WRONG ALL ALONG. It covers three tables:
tab:ksweep, tab:bins, tab:mechanism.
The paper has ELEVEN: family, causes, mechanism, repl, folds, floor, floor2, floorabs, metrics,
ksweep, bins. Eight are guarded by drift alone.
THIS IS THE CLASS OF ERROR THAT ACTUALLY OCCURRED. Three factual errors in the 2026-08-20 rewrite,
I2 89% for 87.5%, condition_gated 0.0829 for 0.0818, and "five of our seven datasets" for eight, were
each a number that was wrong when it was snapshotted, which the inventory check passes by
construction. All three were caught by hand, and hand-checking does not survive the next rewrite.
CLOSE WITH: one table per commit, each against its named artifact:
  tab:floor / floor2 / floorabs   data/results/a2_ladder/floor.json
  tab:repl / tab:folds            the replication pool artifacts
  tab:metrics                     data/results/a3_external/rescored.json
  tab:family                      the n=7 reference family
  tab:causes                      whichever artifact each verdict cites; it is a summary table and may
                                  legitimately have no single one
WHERE A TABLE HAS NO ARTIFACT, say so IN THE MODULE, with the reason, as a NAMED no-op. A silent skip
reads as coverage and is worse than no check at all.

--- D3. RE-VERIFY THE VENUE FACTS.  *** CLOSED 2026-08-21, 51b4c69. *** --------------------------
Every fact UNCHANGED and now recorded with the site's verbatim wording, re-dated in SUBMISSION.md.
The deadline is STILL tentative ("remain subject to final confirmation", every date marked
"Tentative"), so do not read ten days of silence as confirmation. Two facts added that were never
recorded: there are TWO tracks (full 8pp, tiny 4pp) and we are in the full one; and the rest of the
timeline is review 29 Aug-21 Sep, notification 29 Sep, camera-ready 20 Oct, workshop 11 or 12 Dec.
ONE FACT COULD NOT BE RE-VERIFIED and is recorded as unverified rather than left looking checked: the
submit page no longer names a .sty, it links a Google Drive zip that cannot be fetched from here. Our
neurips_2026.sty has not drifted locally (sha256 cedbda3f..., mtime 3 Aug). Re-diffing it against the
workshop zip is a 2-minute HUMAN step, now written down with why it matters.
SUBMISSION.md's venue table is dated 2026-08-11 and its own deadline row says the site marks 29 Aug
"subject to final confirmation, so re-check". Ten days have passed and nobody re-checked.
CLOSE WITH: re-fetch https://icbinb-bio.github.io/submit/ and re-confirm the deadline, the page limit,
double-blind, the full-paper track, and that neurips_2026.sty is still the file the workshop ships.
Update the table WITH A NEW DATE whatever the answer is. If a fact moved, fix the paper first, then
flag it at the top of RESULTS_SUMMARY.md. If the site is unreachable, record the attempt and its date.
An unchecked table that LOOKS checked is the thing to avoid.

--- D4. AUDIT ALL AMENDMENTS.  *** CLOSED 2026-08-21, 9a55284. FIRED RAIL 4. *** -----------------
docs/prereg-audit-2026-08-21.md, one verdict per amendment with the artifact that settles each, kept
ones included. COUNT CORRECTION: this file said "eight amendments" and "Amendments 1 to 8". There is
no Amendment 1 and there are TEN sections -- 4a, 4b and an unnumbered 2026-08-03 one are separate
registrations. Seven of ten clean, including the ones hardest to keep against interest.
FINDING 1: tab:repl was the SUPERSEDED table. Its numerators were digit-for-digit the
pre-Amendment-2 provenance (Frangieh 216, Norman 102, Replogle 12, Datlinger 6), so it marked
Replogle "dropped" while the results section two paragraphs below trains three Replogle sets and
pools them. Rebuilt from the DE_stats_v2 artifacts; appendix-only, body gate untouched.
FINDING 2: see the rail-4 block at the top of this file. Amendment 3.1's promised K=128-subset pooled
estimate was never computed while the paper claimed it was reported. Also, the K labels were wrong:
measured from the built bases, Tian CRISPRi is 32, Tian CRISPRa is 16 and NORMAN IS 16, where the
paper labelled only "Tian at K=32". Norman is the only negative dataset anywhere.
A HARNESS BUG THIS EXPOSED: check_paper.sh ran its build/undefined/overfull/page-gate heredoc BARE.
With no set -e, a failing page gate printed FAIL and the script exited 0 if the later gates passed.
The most important gate in the file was advisory. Its exit code is now captured.
The devil's advocate pass found a pre-registration promise that was never kept: Amendment 6.5 said the
floor would be labelled as the pipeline's rather than the typed encoder's, and until 2026-08-20 it was
not. That was found BY HAND, by reading one amendment. There are eight.
CLOSE WITH: for each of Amendments 1 to 8, extract what it PROMISED, a primary, a family size, a
decision rule, a labelling commitment, and check that the artifact and the paper deliver it. Write
docs/prereg-audit-2026-08-21.md with one verdict per amendment, quoting the promise and naming where
it is kept, or reporting plainly that it is not.
RECORD THE KEPT ONES TOO. This project's credibility rests on four self-reported retractions, and an
audit that finds nothing is evidence only if it could have found something.
Anything unkept is a PAPER fix, never a prereg edit. Rail 7: the pre-registration is frozen.

=======================================================================
NOT AUTONOMOUS — do not attempt these
=======================================================================
THE SUBMISSION. paper/icbinb/ is finished: 8pp body, clean build, style file byte-identical to the
workshop's, repo scrubbed of six deanonymising strings, deadline verified. Filing needs a human:
  1 create the anonymous.4open.science mirror, paste the URL in paper/icbinb/main.tex, rebuild,
    confirm the body still ends on page 8. This comes FIRST — the venue's blinding requirement covers
    linked material.
  2 upload main.pdf at openreview.net/group?id=NeurIPS.cc/2026/Workshop/ICBINB-BIO, full-paper track.
Checked on this machine 2026-08-11 and recorded as a probe table in paper/icbinb/SUBMISSION.md: no
OPENREVIEW_* env vars, no ~/.openreview, openreview-py not installed, no credential store, no gh CLI.
Neither step is reachable from here even in principle. Do not spend turns re-establishing this.

THE SEALED SPLIT (L7). Rail 1. Deliberately left open. It is a one-shot pre-registered test of a
POSITIVE hypothesis and spending it to re-confirm a null buys no information. This is now a live
forward commitment rather than a hypothetical: a positive that survives both corrections DOES exist
(untyped_gnn on the reference screen, +0.0043). Whether to spend the sealed split on it is a decision
for a human, not an agent.

=======================================================================
ALREADY CLOSED — do not redo these
=======================================================================
L0   False RPE1 paragraph in Limitations        FIXED, replaced by the Amendment-2 account.
L0b  Main text over 8pp                         CLOSED; body exactly 8pp, References open page 9.
L1   "One dataset"                              CLOSED. EIGHT datasets. h2a pooled over the seven
     replication datasets: +0.0018, RE 95% CI [-0.0028,+0.0065], I2 = 39.2%, Q p = 0.13.
L1-PRE / L1-CHAIN                               CLOSED; coverage 94-100%, seven per-dataset stages.
L4   Difficulty vs realisation noise            CLOSED 2026-08-14. h2a sd_seed 0.01067 (26 df) >
     sd_level 0.00329 (3 df) > sd_redraw 0.00034 (2 df). The TRAINING SEED is the largest term.
L5   Cause D, baseline not graph-free           DONE and integrated (-0.0036 under correction).
n=7 reference family                            DONE. h2a -0.0120 and promotion_margin +0.0043 both
     SURVIVE BOTH; h2b +0.0077 and h1 -0.0043 do not.
A1   Why edge typing hurts                      CLOSED 2026-08-18. D1 +0.0004 (bonf 1.000) and
     D2 +0.0065 (bonf 0.182), both null: not the parameter count, not the labels.
     data/results/screening_a1/a1_mechanism.json, paper appendix app:mechanism.
A2(a) The empirical detection floor             CLOSED 2026-08-19. Floor <= 0.02 response SDs; the
     scrambled control at 0.40 is NOT recovered (-0.0001, CI width 0.004). 48 lanes, 0 failures.
     data/results/a2_ladder/floor.json, paper appendix app:floor.
A2(b) Simulation power                          CLOSED 2026-08-16. ~250 datasets to detect +0.0043
     across datasets; MDE 0.0075 at n=7 on the frozen fold, 0.0185 across re-draws.
A3   Commensurability with others' metrics      CLOSED 2026-08-16, and it fired the contradiction stop.
     The untyped contrast is -0.0424 at k=20 (GEARS) and +0.0089 over all genes (TxPert), both
     clearing both corrections over 20 cells. paper appendix app:metrics.
A4   Bound the architecture claim                CLOSED 2026-08-16. Search spread 0.0089 against 0.0147
     from seed noise alone; its best cell would need 12 seeds and was run at 1.
A5   Rationale-audit hedges                      CLOSED 2026-08-16. Necessity's "49%" is over a control
     of 3e-07; cause E's source ablation is largely an edge-count effect (STRING leads HuRI by 1.7x
     per edge, not ~200x).

B2   Where in the ranking the graph helps  CLOSED 2026-08-19. Amendment 8. The deficit is CONFINED to
     each perturbation's top-20 genes (-0.0424, 0/5 seeds positive, clears both over 36 cells); every
     interval from rank 251 to 10,282 is positive and clears both. The cumulative crossover at 250-500
     is an artifact of accumulation - the per-interval effect is already positive in 5/5 seeds by rank
     101. At DECILE resolution the harm vanishes entirely (+0.0064, clears nothing), which is the
     transferable lesson and is now checklist item 14.
     data/results/b2_deciles/deciles.json, paper tab:bins in app:metrics.
B5   The abstract sentence                     CLOSED 2026-08-19. In the abstract; 371 chars came from
     MOVING the replication elaboration to app:repl. abstract_plain.txt now has a GENERATOR
     (paper/icbinb/make_abstract_plain.py) and check_paper.sh gates on --check, so it cannot drift again.
B6   Harness and docs consistency              CLOSED 2026-08-19. reproduction.md section 6 documents all
     15 undocumented modules/scripts with the command that reproduces each. Every flag was run with
     --help before being written down. ./init.sh: 630 tests, 0 failures.

CORRECTIONS ON RECORD — do not silently re-reverse any of these:
 - Norman (-0.0790, survives both) overturned the "untyped positive on 5/5 datasets" pooled claim.
   Over eight datasets random-effects is +0.0091 [-0.0024,+0.0207], p=0.12: NO pooled benefit. Three
   per-dataset contrasts clear correction and DISAGREE IN SIGN (+0.0043, +0.0675, -0.0790).
 - h2b SURVIVED at n=6 (bonf 0.0491) and does NOT at n=7 (bonf 0.1308).
 - An interim L4 run with one cell at n=1 gave the OPPOSITE verdict to the n=5 result.
 - The 0.80/0.10 re-draws identify h1's within-level variance, NOT h2a's.
 - The between-re-draw variance component is NOT "at least as large as" the between-seed one, as the
   paper said before 2026-08-16. Measured: 0.0003 against 0.0107.
 - The parameter cut in typed_shared is 34% of the model, not 47%. The 47% came from the six-gene test
   fixture, where the complex-embedding table is one row.
 - "Refuted within the searched space" was never established by the 14-cell architecture search, for
   normalisation or for anything else it tried. A4 measured why.
 - abstract_plain.txt is DERIVED from main.tex and had drifted a full campaign behind it, still
   carrying the retracted "positive on all five independent datasets". Regenerate it after any
   abstract edit.

=======================================================================
HOW TO RUN ANY OF THIS
=======================================================================
  ./run_replication_prep.sh <dataset> <pinnacle-ctx|none> <K>     stages 2-6, idempotent, CPU
  ./run_replication_campaign.sh                                   all replication lanes, skips landed
  ./run_l4_finish.sh / ./run_l4_finalise.sh                       L4 lanes; finalise waits + analyses
  .venv/bin/python merge_registry_n7.py                           BEFORE aggregating a FRESH root
  PYTHONPATH=src .venv/bin/python -m tcell_pipeline.replication.pool [--with-reference]
  PYTHONPATH=src .venv/bin/python -m tcell_pipeline.screening.variance_decomposition --contrast h2a
  ./init.sh                                                       full verification

MANDATORY: source run_replication_stage.sh before ANY manual replication stage (the wrappers do it).
It pins the feature stores — without it INTERMEDIATE_ROOT isolation points them at an empty sandbox and
every target silently gets a ZERO vector while the arm still reports a number — redirects SPLITS_ROOT
away from the frozen data/splits/, and reads CONDITIONS from the DE provenance (its default is the
reference vocabulary, and every replication lane dies on its first batch without it).

MANDATORY: merge_registry_n7.py before aggregating a fresh screening root, or the report carries a
false "NOT comparable" flag. The reference registry is data/results/experiment_registry.yaml, NOT
inside data/results/screening/ — a copy step pointed at the latter fails silently.

--- SHARED-BOX REALITY (measured 2026-08-12, affects every ETA in this file) -------------------
This machine is shared and the contention is not visible from nvidia-smi alone. Measured while the L4
lanes ran:
  - `python load_data.py Thyroid` at 5001% CPU (about 50 of 64 cores), load average 123 and rising.
  - 5+ `run.py --encoding ...` quantum-ML jobs at ~99.5% CPU each.
  - Earlier, `launch.py --config configs/pfdv2.yaml --gpu 2` held 80,948 MiB of card 2's 81,920 - a
    CUDA context could not even be created there.
CONSEQUENCES for planning:
  1 These lanes are CPU-bound on row-by-row subgraph sampling (see UntypedGraphEncoder's docstring:
    "the batch is still sampled row-by-row on CPU, which is now the throughput floor"). Each lane uses
    about ONE core. GPU utilisation reads 100% and means very little.
  2 Per-epoch time therefore tracks whoever else is on the box: the same typed_static lane measured
    ~38 min/epoch when quiet, ~45 with three of ours running, ~80 once the Thyroid job appeared, and
    ~90 when six of ours competed. Quote ETAs as ranges and re-measure rather than extrapolating.
  3 ALWAYS map CUDA_VISIBLE_DEVICES to physical cards with torch's mem_get_info before scheduling.
    nvidia-smi index 3 is a T400, not an A100, and smi ordering is not CUDA ordering.
  4 Adding a worker is only worth it when the marginal CPU cost is small. Going 3->4 of our lanes costs
    one core out of 64 and gains a GPU; going 4->6 cost throughput outright.
  5 A FREE CARD DOES NOT STAY FREE. run_l4_card2.sh preflighted card 2 at 78 GiB free, started a lane,
    and 2h47m later a co-tenant reclaimed the card: the lane was SIGTERMed (exit 143) and the next four
    died instantly with CUDA OOM at "161 MiB free". A preflight is necessary and not sufficient. Any
    opportunistic worker on a shared card must (a) release its claim on failure so the main scheduler
    can retry - run_l4_card2.sh does - and (b) be treated as best-effort, never as the only path to a
    lane. Do not restart such a worker without re-checking the card; ours is at 68 GiB used again.

=======================================================================
INTEGRATION + REPORTING
=======================================================================
- Into the paper only at n>=4 both arms, live gates, family_size=4, every number RE-DERIVED from the
  persisted artifact rather than a log or a prior claim.
- When a result contradicts something already in the paper, FIX THE PAPER and record the retraction in
  RESULTS_SUMMARY.md. This project's credibility rests on having reported four of its own so far
  (h2a at n=2, h2b at n=2, h1 at family_size=1, and the Amendment-2 rule error).
- Abstract, causes table and checklist stay in sync with the body; a cause whose verdict changes
  changes in all three places.
- As limitations close, the Limitations section SHRINKS and Results grows. That is the visible signal
  the plan is working. Re-check after each closure that no other section still concedes the closed point.
- After every paper edit: 0 errors, 0 undefined, 0 overfull >2pt, 0 em/en dashes, 0 banned words, main
  text EXACTLY 8pp.
- RESULTS_SUMMARY.md and session-handoff.md stay current: n, per-seed values, both corrections,
  family_size, gate health, epochs, and the name and reason of any dropped seed.

ORDER OF WORK (rewritten 2026-08-21, after the D-series closed). NOTHING IS RUNNING.
THE WHOLE D-SERIES AND C1a ARE CLOSED. The A- and B-series were already closed. What is left is:

  1 C1b IS RUNNING. Launched 2026-08-21 02:08 on cards 0/1/3 (card 2 held by a co-tenant, its CUDA
    context could not even be created, so the preflight skipped it). 48 lanes queued into a FRESH
    root data/results/c1_ladder: 24 expression_only and 24 condition_gated over the six existing
    rungs at seeds 0-3, all at --lambda-graph 0.
      watch:    data/logs/c1_ladder.nohup.log and data/logs/c1_ladder/*.log
      order:    SEED-MAJOR on purpose. A stopped campaign then has a BALANCED ladder at whatever n
                landed, not three rungs at n=4 and three at n=0 -- which the floor rule cannot read
                at all, since the floor is the smallest rung that clears AND is cleared by every
                LARGER one. Amendment 9.9 anticipates being stopped short; this makes that outcome
                readable instead of wasted.
      gate:     every condition_gated lane echoes gate_mean_min. Below 1e-3 it is UNDECIDABLE under
                Amendment 3.4 and must SHRINK n, never be counted as evidence.
      read it:  ladder_report --root data/results/c1_ladder --arm condition_gated
                --reference-root data/results/screening_lambda0
      RE-MEASURE per-epoch cost on the first condition_gated lane rather than trusting 7.5-16 GPU-h.
      If the measured rate puts the campaign past 29 Aug, STOP and report at whatever n landed,
      labelled preliminary with its n (rail 5 and Amendment 9.9). Do not quietly extend.
  2 The rail-4 finding at the top of this file. NOT autonomous. Whether the paper's
    "sign we cannot predict" argument should change is a scientific call on its central replication
    claim, eight days out. Flagged and left, which is what rail 4 says to do.
  3 The style-file re-diff (D3). A human step, 2 minutes, before upload.

Box state measured 2026-08-21 00:54 and not re-checked since: cards 0/1/3 free at 78/78/75 GiB, card
2 held by a co-tenant at 63 GiB, LOAD AVERAGE 102 on 64 cores from other users' CPU jobs. These lanes
are CPU-bound on subgraph sampling, so price C1b at the SLOW end of the measured 38-90 min/epoch
until a real lane says otherwise. RE-CHECK THE CARDS before launching; a free card does not stay free.

B3 is answered without running. B4 is deprioritised on purpose. B1b-d are declined on power under
Amendment 7.3, and overturning that means running them AND re-correcting at m=4, where D3 does not
clear.

RE-MEASURE per-epoch cost on the first lane of any new campaign rather than extrapolating: the same
lane has measured 38 to 90 min/epoch on this box depending on who else is running.

USE THE RUNNERS RATHER THAN HAND-LAUNCHING. run_a1_mechanism.sh takes ARMS from the environment, skips
landed lanes, takes an atomic claim per lane and now SELECTS usable cards instead of refusing the whole
run when one is busy. That last fix exists because the earlier version cost eleven and a half hours of
idle A100 time when a co-tenant held one card of four.

WATCH OUT FOR THE WATCHER. A `pgrep -f "<pattern>"` in a waiter matches any process whose command line
contains the string, including the shell that WROTE the script with a heredoc. run_ladder_finalise.sh
waited on its own creator and never fired. Use the bracket form, `pgrep -f "[r]un_screening --only"`,
or pass the pattern as a function argument.

WHAT SUCCESS LOOKS LIKE. The paper already says the typed graph does not help across eight datasets and
five cell types, that the instrument recovers an injected signal at 0.02 response SDs while a scrambled
control at 0.40 is not recovered, and that the damage is neither the parameter count nor the annotation.
Success for THIS file is one more sentence: and here is the component of the message form that does it.
If B1 finds no single component, success is saying that instead, with the arms that failed to find one.
