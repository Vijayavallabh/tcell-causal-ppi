Take feat-013 (Reproducibility Verification) as far as governance ALLOWS: build the artifact manifest from
a REAL run and author the 11 fallacy probes from REAL diagnostics. Repo
/mnt/md0/IITM/BackUp/Home/vijayavallabh/tcell-causal-ppi, Python 3.12 + uv, PYTHONPATH=src.
./init.sh green at every stopping point.

============================================================
READ THIS TWICE — feat-013 CANNOT REACH "done" THROUGH YOU
============================================================
Its terminal state needs a sealed confirmatory decision, and opening the sealed challenge split is
TEST-STEWARD-ONLY. It is never done from an agent session. `verify_reproducibility` returning
CANNOT_VERIFY is the CORRECT answer here, not a failure to fix, and you must not engineer it into
REPRODUCIBLE by supplying inputs that do not exist. Your job is to take it to "everything except the
sealed step", record precisely what remains and WHO must do it, and leave the feature in-progress.
The same boundary is why H1's confirmatory verdict is not yours to produce: everything in this repo is
DEVELOPMENT-fold evidence.

============================================================
THREE OTHER CLAUDE SESSIONS ARE LIVE IN THIS SAME CHECKOUT
============================================================
Session A: feat-006 (tabular baselines) — GPU 2 + 48 CPU cores until ~23:00.
Session B: feat-008 (Stage-B calibration + rationale fit loops).
Session C: feat-005 (basis study).
You are session D. Boundaries are HARD.

YOU OWN:
  src/tcell_pipeline/reproducibility/__init__.py
  src/tcell_pipeline/reproducibility/verify.py
  src/tcell_pipeline/reproducibility/fallacy_scan.py
  src/tests/test_reproducibility.py
  ...plus any NEW file inside src/tcell_pipeline/reproducibility/ (put the manifest builder in
  reproducibility/manifest.py and your real-run entry point in reproducibility/run_repro_real.py).

NOT YOURS — read/import only, never write:
  src/tcell_pipeline/run_module8_real.py  (session A's; its run_repro() is the OLD driver. Do NOT edit it.
    Build your entry point inside your own package instead; session A rewires run_repro() to call you.)
  src/tcell_pipeline/evaluation/metrics.py, evaluation/metrics_ref.py, baselines/*, probe_catboost_budget.py
  src/tcell_pipeline/model.py, training/*, rationale/*            (session B)
  src/tcell_pipeline/programs/*                                    (session C)
  src/tests/test_baselines.py, test_comparators.py, test_metrics.py, test_training.py, test_rationale.py,
  test_programs.py

THE DoD TRIAD IS SHARED — DO NOT EDIT feature_list.json, progress.md, session-handoff.md. Four sessions
doing read-modify-write on one JSON silently loses updates; there is no merge. Write your evidence to a NEW
file, docs/feat013-repro-notes.md. Session A merges it at commit time.

COMMITS: do not commit until I say so; then only your own files by explicit path (`git commit -- <paths>`).
Never `git commit -a`, never `git add .`, never stage the triad.

============================================================
WHAT ALREADY EXISTS (do not rebuild it)
============================================================
- reproducibility/verify.py  -> verify_reproducibility(checkout, manifest, config_snapshot=...), VERDICTS =
  (REPRODUCIBLE, PARTIALLY_REPRODUCIBLE, NOT_REPRODUCIBLE, CANNOT_VERIFY)
- reproducibility/fallacy_scan.py -> run_fallacy_scan + all 11 DETECTORS already implemented:
  base_rate, berkson, collider, correlation_not_causation, ecological, garden_of_forks, look_elsewhere,
  regression_to_mean, reverse_causation, simpson, survivorship
The detectors exist. What is MISSING is their real INPUTS, and a manifest built from a real run.

============================================================
WHAT TO BUILD
============================================================
(1) MANIFEST FROM A REAL RUN. Hash the frozen preprocessing artifacts. The critical distinction, which the
    existing driver already half-implements and which you must keep rigorous:
      - An INDEPENDENTLY FROZEN expected hash (e.g. one published in data/splits/manifest.json at freeze
        time) compared against today's file IS a genuine reproduction test.
      - A SELF-DERIVED hash — hashing the file now and "checking" it against itself — proves only that the
        file is readable. It is NOT a reproduction test and every such entry must be LABELLED as such in
        the manifest and in the report. Do not let a self-derived entry read as a passed check.
    Do NOT hash live checkpoints under data/checkpoints/: session B is writing there right now and you
    would hash a half-written file. Stick to the frozen preprocessing artifacts.

(2) AUTHOR THE 11 FALLACY PROBES FROM REAL DIAGNOSTICS. Each detector needs inputs drawn from this
    project's actual numbers, not toy arrays. The material is real and specific:
      - look_elsewhere / garden_of_forks: the multiseed campaign tested four contrasts simultaneously; a
        raw p=0.0208 was published as a resolved positive and then FAILED Bonferroni. See
        screening/multiseed.py, which names fallacy_scan as the thing that exists to catch this.
      - regression_to_mean: this is a near-null-signal regime; a model collapsing to the training mean was
        being scored +0.0129 off floating-point dust until session A fixed the metric today.
      - survivorship / base_rate: 385 train and 91 val rows are off-graph and carry an all-zero feature
        vector; blocked-target OOD means val targets are disjoint from train.
    Where a probe genuinely has no real input in this project, say so and mark it Unevaluable — an honest
    "not applicable here" beats a fabricated input. Unevaluable is already a concept in the code.

(3) RECORD THE HONEST VERDICT. CANNOT_VERIFY, with its reason stated, plus a precise statement of what
    remains and who must do it (the test steward, on the sealed split).

============================================================
THE FRAME
============================================================
This project's headline result is a NEGATIVE and that is fine. Do not tune anything to rescue it.
- A comparison you did not compute is not a result.
- Absence of evidence must report None/unknown, NEVER a pass. This feature is entirely about that
  distinction: a missing sealed decision is "cannot verify", not "verified".
- A guard whose input is a CONSTANT can only confirm. Ask of every check you write: what input would make
  this FIRE? If none can exist, it is decoration. Then trace the value to the report, the JSON, AND the
  process exit code — a run that prints CANNOT_VERIFY must not exit 0 pretending success.
- Red-first TDD: watch every test pinning a correctness claim FAIL first, and CONSTRUCT an input that
  defeats correctness-critical code. Read AGENTS.md.

============================================================
SHARED MACHINE AND SAFETY
============================================================
- This work is cheap and CPU-light (hashing + small numpy checks). Set OMP_NUM_THREADS=4 and launch
  nothing wide before ~23:00 — session A holds 48 cores. Run nvidia-smi before any GPU; you should not
  need one.
- NEVER run evaluation/sealed_eval.py. The sealed challenge split (5,608 rows) is SEQUESTERED and
  UNOPENED, and this feature is precisely the one that might tempt you to open it. Do not.
- NEVER run run_module0.py (DESTRUCTIVE — overwrites the frozen source marts).
- Never run tcell_pipeline.programs.run_program_basis: it OVERWRITES the frozen program basis every other
  session depends on. You only ever HASH that file, never regenerate it.
- Never modify data/results/screening/promoted.json.
- Write only under data/results/reproducibility/.

============================================================
DONE WHEN
============================================================
The manifest is built from real artifacts with every entry labelled independent vs self-derived; the 11
probes are authored from real diagnostics or explicitly marked Unevaluable with a reason; the honest
CANNOT_VERIFY verdict is recorded with its cause; the exit code reflects it; ./init.sh is green;
docs/feat013-repro-notes.md names the steward-only remainder; feat-013 stays IN-PROGRESS; nothing outside
your owned files changed; and you have STOPPED for me to review before any commit.
