# Python 3.12 — see README for the uv setup.
# Grouped by role. The core stack (Tier 0: data audit + linear baselines) runs CPU-only;
# torch / torch-geometric are needed for the Tier 1 graph model (EG-IPG).

# --- Single-cell data I/O ---
scanpy
anndata
mudata
h5py            # backed / lazy HDF5 reads of the large .h5ad / .h5mu artifacts

# --- Scientific & tabular stack ---
numpy
scipy
pandas
polars          # fast columnar ops on the derived mart
pyarrow         # Parquet shards (perturbation_condition, program_response, protein_edges, ...)
pyyaml          # feature_availability.yaml leakage-fence manifest (feature_availability.py)
scikit-learn    # linear baselines + program bases (PCA / NMF / ICA, ridge, elastic-net, GBM)
mygene
catboost        # feat-006 tabular bar: MultiRMSE fits all K programs in ONE model (Apache-2.0, ~97 MB)
tabicl          # feat-006 tabular foundation-model bar (BSD-3). Downloads a ~114 MB checkpoint on first
                # use and wants a GPU. NOT TabPFN: its current weights are non-commercial and its own docs
                # say competitive benchmarking needs a commercial licence — a governance blocker for a
                # published comparator, on top of a login token and 128 single-target refits.
# --- Graphs & modeling (Tier 1: EG-IPG) ---
# GPU note: this host's NVIDIA driver is 535/CUDA 12.2, so install the CUDA-12.6 torch build
# (runs on 12.2 via minor-version compat) — the default cu13x wheel can't see the GPU:
#   uv pip install --index-url https://download.pytorch.org/whl/cu126 "torch==2.13.0+cu126"
torch
torch-geometric # or DGL; compiled extras (torch-scatter/-sparse) via PyG's wheel index if needed
networkx        # network-propagation baselines and graph diagnostics
fair-esm        # ESM-2 650M PLM embeddings (embeddings_plm.py); PINNACLE embeds via Figshare download

# --- Visualization ---
matplotlib
seaborn

# --- Notebooks ---
jupyter
ipykernel

# --- Tests (init.sh runs pytest) ---
pytest

# --- Optional / later phases (uncomment as needed) ---
# gseapy        # pathway / program enrichment recovery (mechanism metrics)
# mapie         # conformal prediction / calibrated intervals
# wandb         # experiment tracking
