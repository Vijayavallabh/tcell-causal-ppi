Finish feat-005 (Latent Program Extraction) by running the method x K BASIS STUDY and building the
shallow-VAE basis — the only thing left on that feature. The extraction machinery is already DONE and the
production basis is FROZEN. This is a CHARACTERISATION STUDY, not a licence to change anything.
Repo /mnt/md0/IITM/BackUp/Home/vijayavallabh/tcell-causal-ppi, Python 3.12 + uv, PYTHONPATH=src.
./init.sh must stay green at every stopping point.

============================================================
READ THIS FIRST — TWO OTHER CLAUDE SESSIONS ARE LIVE IN THIS SAME CHECKOUT
============================================================
Session A is finishing feat-006 (tabular baselines): a GPU job on GPU 2 plus a 48-core CatBoost probe,
both running until roughly 23:00.
Session B is building feat-008 (Stage-B calibration + rationale fit loops).
You are session C, in the SAME working tree, at the SAME time. The boundaries below are HARD.

FILES SESSION A OWNS — read/import only, never write:
  .gitignore, requirements.txt,
  src/tcell_pipeline/baselines/__init__.py, src/tcell_pipeline/baselines/simple_baselines.py,
  src/tcell_pipeline/evaluation/metrics.py, src/tcell_pipeline/evaluation/metrics_ref.py,
  src/tcell_pipeline/run_module8_real.py, src/tcell_pipeline/probe_catboost_budget.py,
  src/tests/test_baselines.py, src/tests/test_comparators.py, src/tests/test_metrics.py

FILES SESSION B OWNS — read/import only, never write:
  src/tcell_pipeline/model.py,
  src/tcell_pipeline/training/trainer.py, training/losses.py, training/run_train.py,
  src/tcell_pipeline/rationale/rationale_head.py, rationale/rationale_loss.py,
  src/tests/test_training.py, src/tests/test_rationale.py

FILES YOU OWN:
  src/tcell_pipeline/programs/program_basis.py
  src/tcell_pipeline/programs/run_program_basis.py
  src/tests/test_programs.py
  ...plus any NEW file you create (put the study itself in a new module, e.g.
  src/tcell_pipeline/programs/basis_study.py).

EXPLICITLY NOT YOURS even though it sits in your package:
  src/tcell_pipeline/programs/program_decoder.py — session B's model.py imports ProgramDecoder. Changing
  its interface breaks their in-flight work. You do not need it for this study.

Your new reconstruction / sparsity / stability metrics go in YOUR new module. Do NOT add them to
src/tcell_pipeline/evaluation/metrics.py — that file is session A's and is the frozen primary-endpoint
module.

THE DoD TRIAD IS SHARED STATE — DO NOT EDIT IT: feature_list.json, progress.md, session-handoff.md.
Three sessions doing read-modify-write on one JSON file silently loses updates; there is no merge. Write
your evidence to a NEW file, docs/feat005-basis-study-notes.md, in the style you would have used for the
feature_list evidence block. Session A merges it at commit time.

COMMITS: do not commit until I say so. Then commit ONLY your own files by explicit path
(`git commit -- <paths>`). Never `git commit -a`, never `git add .`, never stage the triad.

============================================================
THE ONE THING THAT WOULD DESTROY THE WHOLE PROJECT
============================================================
`data/intermediate/gene_program_loadings.parquet` is the FROZEN production basis B (10282 x 128,
sparse_pca). EVERY result in this project is expressed in its coordinates: the frozen H1, the 5-seed
campaign, promoted.json, all of session A's baselines, and everything session B is fitting right now.

`python -m tcell_pipeline.programs.run_program_basis` OVERWRITES that file, and so does any call to
`save_program_basis()` at its default path. AGENTS.md currently lists that command as "safe to re-run
anytime" — that is true only in the narrow sense that the default sparse_pca/K=128 fit is deterministic.
Run it with a DIFFERENT method or K and you silently replace the frozen basis with an incompatible one,
invalidating every number above and both other sessions' running work.

THEREFORE:
- Do NOT run run_program_basis at all.
- Do NOT call save_program_basis / save_program_response with default paths.
- Your study fits every candidate basis IN MEMORY and writes ONLY under data/results/basis_study/.
- If the study finds some other method or K looks better, that is a FINDING. Report it and STOP.
  Changing the frozen basis is my call, not yours, and not part of this goal.
- Before you finish, verify the frozen basis is untouched against the INDEPENDENTLY RECORDED fingerprint
  below — taken at 2026-07-20 19:00 before you started, so a match is a real check and not a file compared
  to itself. Absence of a check is not evidence it survived.

    sha256 (first 32)                 mtime                file
    d5a8883da20dd8ef8824f565188c5f45  2026-07-16 11:07:17  data/intermediate/gene_program_loadings.parquet
    2c29ba9535e293dd003836f3aad50d42  2026-07-16 11:07:17  data/intermediate/program_response.parquet

  Check it at START and at END:
    sha256sum data/intermediate/gene_program_loadings.parquet data/intermediate/program_response.parquet
  If either has changed, STOP IMMEDIATELY and tell me — do not try to regenerate it.

============================================================
WHAT TO BUILD
============================================================
feat-005's evidence says exactly what remains: "the method x K comparison metrics + shallow-VAE basis
still remain." The machinery exists — `program_basis._factor` already supports sparse_pca / nmf / fastica
/ svd, and `fit_program_basis` handles fold-local fitting on TRAIN rows only.

(1) METHOD x K STUDY. Fit each method at K in {64, 128, 256, 512} on the TRAIN fold only and score:
    - RECONSTRUCTION: centred reconstruction error against the predict-zero baseline (the existing
      evidence quotes MAE 0.687 vs 0.817 for sparse_pca K=128 — reproduce that cell as a sanity check
      that your harness agrees with the recorded number before you trust the other 15).
    - SPARSITY: exact-zero fraction of the loadings (22.7% for the frozen sparse_pca fit) plus a
      dead-program count.
    - STABILITY: you must DEFINE AND STATE THE RESAMPLING PROTOCOL BEFORE RUNNING — how many resamples,
      what fraction, what seed, and how two bases are matched before being compared (a basis is only
      identified up to permutation and sign, so a naive elementwise correlation is meaningless; use a
      matched criterion such as Hungarian matching on |cosine| between components). An unmatched
      stability number is not a stability number.
(2) SHALLOW-VAE BASIS. Build it and score it on the same three axes so it joins the same table.
(3) One comparison table over all method x K cells + the VAE, written to data/results/basis_study/.

============================================================
THE FRAME
============================================================
This project's headline result is a NEGATIVE and that is fine — after multiplicity control no graph
variant reliably beats a no-graph model, and the frozen H1 is at PARITY with no-graph. Do not tune
anything to rescue it. A study that concludes "the frozen choice was unremarkable" is a SUCCESSFUL study.
- A comparison you did not compute is not a result. Never read a ranking off two summary numbers.
- If you compare many cells at once, say so and correct for it (report BOTH Bonferroni and Holm) before
  calling any difference "reliable". 16 cells is a lot of chances to find a winner by luck.
- A degenerate case (a dead program, a rank-deficient fit, a non-converged NMF) is UNDECIDABLE, not
  significant. Absence of evidence reports None/unknown, never a pass.
- Record CONVERGENCE evidence for every fit (NMF/ICA/SparsePCA all iterate and all can hit their cap). A
  method that did not converge is not evidence that the method is worse — say so rather than ranking it.
- Ask of every guard: WHAT INPUT WOULD MAKE THIS FIRE? If none can exist, it is decoration. Trace the
  guarded value to the output, the JSON, and the process exit code.

============================================================
METHOD AND SHARED MACHINE
============================================================
- Red-first TDD: tests pinning a correctness claim must be watched FAILING first; for correctness-critical
  code CONSTRUCT an input that defeats it. Read AGENTS.md. Keep unit tests tiny and synthetic.
- BUILD FIRST, SWEEP LATER. Writing the harness, the matched stability metric and its tests is CPU-light —
  do all of that now. The full 16-cell sweep on the real matrix (21262 x 10282) is NOT cheap: NMF and ICA
  at K=512 can run for hours.
- The box is a shared 64-core machine. Session A is holding 48 cores until ~23:00. Set OMP_NUM_THREADS=4
  and do not launch anything wide before then. Run `nvidia-smi` before touching any GPU (GPU 2 is taken;
  0/1/4 belong to other users) — the VAE is small and should be fine on CPU.
- BEFORE THE FULL SWEEP, or any run over ~1 hour, or any new dependency: STOP and show me the plan — what
  runs, the estimate WITH ITS BASIS (time ONE real cell end-to-end first; a sub-component benchmark is not
  a whole-pipeline estimate — that error nearly launched a 22.7 h campaign here as "~7 h"), the resampling
  protocol, and the statistic you will report. Wait for my approval.

============================================================
HARD SAFETY
============================================================
- The sealed challenge split (5,608 rows) is SEQUESTERED and UNOPENED. NEVER run evaluation/sealed_eval.py.
- NEVER run run_module0.py — DESTRUCTIVE, overwrites the frozen source marts.
- Never modify data/results/screening/promoted.json.
- Fold-locality is the leakage fence: bases are fit on TRAIN rows ONLY. `fit_program_basis` already does
  this — do not widen it. Val/test rows must never enter a factorisation.

============================================================
DONE WHEN
============================================================
The method x K table exists with a NAMED, pre-stated stability protocol and per-fit convergence evidence;
the shallow-VAE basis is built and scored in the same table; the sanity cell reproduces the recorded
sparse_pca K=128 numbers; the frozen basis is verified byte-identical (sha256 recorded before and after);
docs/feat005-basis-study-notes.md holds the evidence; nothing outside your owned files changed; and you
have STOPPED for me to review before any commit.
